# Tool-specific AI entry point

Read and obey `AGENTS.md` before making changes. This is the canonical engine/template repository. Actual controlled documents belong in separate private derived repositories. Then read `AI-GENERATION-CONTRACT.md`, `ai/template-contract.json`, and `ai/document-profiles.json`.
